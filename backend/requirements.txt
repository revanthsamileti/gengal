Flask==3.0.3
flask-cors==4.0.1
agora-token-builder==1.0.0
firebase-admin==6.5.0
requests==2.32.3
# app.py refuses to serve production traffic under app.run() (the Werkzeug dev
# server) and names waitress as the way to run it, so the thing it tells you to
# use has to actually be installed.
waitress==3.0.0
